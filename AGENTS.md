# UAV_Dreamer 协作约定

先阅读 `LEARNING_MAP.md`，区分源码、配置、训练入口和实验输出。

- 不无说明地新增训练脚本或重复算法代码。场景与训练参数优先使用现有 config/preset 和统一入口；新增配置能力与行为变更必须说明理由、影响和验证方式。
- 每次训练实验先汇报：parent checkpoint（精确路径及恢复范围）、修改文件、修改内容、保持不变内容、完整启动命令、全新输出目录、Gate（指标、种子、回合数、阈值、失败处理）。
- 每次修改后展示 git diff 和 git status；训练前必须再次核对并等用户明确确认。不得自动开始训练、续训或自动推进课程。既有脚本包含自动训练行为，不因脚本存在而获得执行授权。
- 项目根目录目前不是 Git 仓库；`dreamerv3/` 是独立仓库。执行 `git -C dreamerv3 diff`、`git -C dreamerv3 status --short`，区分既有修改与本次修改。根目录文件需保留修改前副本，用 `git diff --no-index` 展示差异；新增文件与 `/dev/null` 比较。明确说明 Git 覆盖缺口，不擅自初始化仓库、提交、暂存或清理既有修改。
- 不覆盖 parent checkpoint、历史实验配置、replay 或评测结果；失败分支不得自动成为下一阶段 parent。
- 仅改变场景/超参数时不复制 Agent、RSSM、Actor/Value、replay。观测形状改变需检查模型和数据兼容性，使用新输出目录。
- 当前规则是协作流程约束，不代表代码已实现强制审批锁。
